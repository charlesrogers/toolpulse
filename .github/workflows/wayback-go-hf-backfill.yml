name: Wayback go.hf Backfill

on:
  schedule:
    # Run hourly — 4 parallel slices x 30 URLs = 120 URLs/hour = 2880/day
    - cron: '17 * * * *'
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Number of URLs to backfill per slice per run'
        required: false
        default: '30'

permissions:
  contents: write
  actions: read

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        slice: [0, 1, 2, 3]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 lxml

      - name: Restore database
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: toolpulse-db
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-backfill.yml
          workflow_conclusion: ''

      # Download ALL slice progress files
      - name: Restore go.hf backfill progress (slice 0)
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: go-hf-backfill-progress-slice-0
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-go-hf-backfill.yml

      - name: Restore go.hf backfill progress (slice 1)
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: go-hf-backfill-progress-slice-1
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-go-hf-backfill.yml

      - name: Restore go.hf backfill progress (slice 2)
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: go-hf-backfill-progress-slice-2
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-go-hf-backfill.yml

      - name: Restore go.hf backfill progress (slice 3)
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: go-hf-backfill-progress-slice-3
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-go-hf-backfill.yml

      - name: Restore legacy go.hf backfill progress
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: go-hf-backfill-progress
          path: data/
          if_no_artifact_found: warn
          search_artifacts: true
          workflow: wayback-go-hf-backfill.yml

      - name: Run go.hf Wayback backfill (slice ${{ matrix.slice }})
        env:
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '30' }}
          SLICE: ${{ matrix.slice }}
          WAYBACK_PROXY_URL: https://toolpulse-wayback.charlesrogers.workers.dev/wayback-proxy
        run: python3 scrapers/wayback_go_hf.py --db --batch-size $BATCH_SIZE --slice $SLICE --total-slices 4

      - name: Show database stats
        if: matrix.slice == 0
        run: python3 db.py

      # Only slice 0 uploads the database to avoid conflicts
      - name: Upload database
        if: matrix.slice == 0
        uses: actions/upload-artifact@v4
        with:
          name: toolpulse-db
          path: data/toolpulse.db
          retention-days: 90
          overwrite: true

      - name: Upload go.hf backfill progress (slice ${{ matrix.slice }})
        uses: actions/upload-artifact@v4
        with:
          name: go-hf-backfill-progress-slice-${{ matrix.slice }}
          path: data/go_hf_backfill_progress_${{ matrix.slice }}.json
          retention-days: 90
          overwrite: true
